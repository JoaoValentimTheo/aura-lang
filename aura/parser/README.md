Parser directory: place ANTLR grammar and generated parser here.

Phase 1 tasks:
- Add ANTLR4 grammar file (aura.g4)
- Add instructions to install ANTLR4 runtime
- Provide initial simple grammar with tokens for identifiers, numbers, strings, and basic statements
