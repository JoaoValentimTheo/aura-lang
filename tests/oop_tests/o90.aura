// OOP: Builder Pattern
class ImplC36568EcBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> ImplC36568EcBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> ImplC36568EcBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> ImplC36568Ec {
        return ImplC36568Ec(name: self.name, age: self.age, email: self.email);
    }
}

class ImplC36568Ec {
    public let name: str
    public let age: int
    public let email: str
    
    public def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = ImplC36568EcBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
