// OOP: Mixin-like Composition
class ImplC34864Np {
    public def log(msg: str) { print(msg); }
}

class RealC30132Cv {
    private let logger: ImplC34864Np
    public let value: int
    
    public def new(val: int, logger: ImplC34864Np) {
        self.value = val;
        self.logger = logger;
    }
    
    public def process() -> int {
        self.logger.log("Processing");
        return self.value * 2;
    }
}

let logger = ImplC34864Np();
let obj = RealC30132Cv(val: 50, logger: logger);
print(obj.process());
