// OOP: Builder Pattern
class C13609XzBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> C13609XzBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> C13609XzBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> C13609Xz {
        return C13609Xz(name: self.name, age: self.age, email: self.email);
    }
}

class C13609Xz {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = C13609XzBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
