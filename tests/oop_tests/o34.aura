// OOP: Builder Pattern
class ConcreteC93247AuBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> ConcreteC93247AuBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> ConcreteC93247AuBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> ConcreteC93247Au {
        return ConcreteC93247Au(name: self.name, age: self.age, email: self.email);
    }
}

class ConcreteC93247Au {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = ConcreteC93247AuBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
