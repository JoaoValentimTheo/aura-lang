// OOP: Builder Pattern
class BaseC12223BqBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> BaseC12223BqBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> BaseC12223BqBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> BaseC12223Bq {
        return BaseC12223Bq(name: self.name, age: self.age, email: self.email);
    }
}

class BaseC12223Bq {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = BaseC12223BqBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
