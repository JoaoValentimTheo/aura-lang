// OOP: Mixin-like Composition
class MockC15137Ch {
    public def log(msg: str) { print(msg); }
}

class AbstractC69998Au {
    private let logger: MockC15137Ch
    public let value: int
    
    public def new(val: int, logger: MockC15137Ch) {
        self.value = val;
        self.logger = logger;
    }
    
    public def process() -> int {
        self.logger.log("Processing");
        return self.value * 2;
    }
}

let logger = MockC15137Ch();
let obj = AbstractC69998Au(val: 50, logger: logger);
print(obj.process());
