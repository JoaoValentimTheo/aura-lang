// OOP: Builder Pattern
class BaseC26606FcBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> BaseC26606FcBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> BaseC26606FcBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> BaseC26606Fc {
        return BaseC26606Fc(name: self.name, age: self.age, email: self.email);
    }
}

class BaseC26606Fc {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = BaseC26606FcBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
