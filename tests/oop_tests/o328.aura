// OOP: Builder Pattern
class ImplC14366GlBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> ImplC14366GlBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> ImplC14366GlBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> ImplC14366Gl {
        return ImplC14366Gl(name: self.name, age: self.age, email: self.email);
    }
}

class ImplC14366Gl {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = ImplC14366GlBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
