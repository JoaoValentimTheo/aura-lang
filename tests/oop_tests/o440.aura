// OOP: Builder Pattern
class MockC77745ZiBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> MockC77745ZiBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> MockC77745ZiBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> MockC77745Zi {
        return MockC77745Zi(name: self.name, age: self.age, email: self.email);
    }
}

class MockC77745Zi {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = MockC77745ZiBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
