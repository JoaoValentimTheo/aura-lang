// OOP: Builder Pattern
class MockC69846WpBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> MockC69846WpBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> MockC69846WpBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> MockC69846Wp {
        return MockC69846Wp(name: self.name, age: self.age, email: self.email);
    }
}

class MockC69846Wp {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = MockC69846WpBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
