// OOP: Delegation
class AbstractC62905Sv {
    public let data: int = 0
    public def process() -> int { return self.data * 2; }
}

class ConcreteC51071If {
    private let delegate: AbstractC62905Sv
    
    public def new(t: AbstractC62905Sv) { self.delegate = t; }
    
    public def process() -> int {
        return self.delegate.process() + 10;
    }
}

let target_obj = AbstractC62905Sv();
target_obj.data = 25;
let delegator_obj = ConcreteC51071If(t: target_obj);
print(delegator_obj.process());
