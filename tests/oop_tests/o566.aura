// OOP: Builder Pattern
class RealC11332AyBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> RealC11332AyBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> RealC11332AyBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> RealC11332Ay {
        return RealC11332Ay(name: self.name, age: self.age, email: self.email);
    }
}

class RealC11332Ay {
    public let name: str
    public let age: int
    public let email: str
    
    public def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = RealC11332AyBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
