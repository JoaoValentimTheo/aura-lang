// OOP: Mixin-like Composition
class AbstractC13812Qf {
    public def log(msg: str) { print(msg); }
}

class ImplC30702Fp {
    private let logger: AbstractC13812Qf
    public let value: int
    
    public def new(val: int, logger: AbstractC13812Qf) {
        self.value = val;
        self.logger = logger;
    }
    
    public def process() -> int {
        self.logger.log("Processing");
        return self.value * 2;
    }
}

let logger = AbstractC13812Qf();
let obj = ImplC30702Fp(val: 50, logger: logger);
print(obj.process());
