// OOP: Builder Pattern
class ImplC98779KmBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> ImplC98779KmBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> ImplC98779KmBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> ImplC98779Km {
        return ImplC98779Km(name: self.name, age: self.age, email: self.email);
    }
}

class ImplC98779Km {
    public let name: str
    public let age: int
    public let email: str
    
    public def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = ImplC98779KmBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
