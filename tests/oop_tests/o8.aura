// OOP: Mixin-like Composition
class ImplC60456Ur {
    public def log(msg: str) { print(msg); }
}

class C33762Ka {
    private let logger: ImplC60456Ur
    public let value: int
    
    public def new(val: int, logger: ImplC60456Ur) {
        self.value = val;
        self.logger = logger;
    }
    
    public def process() -> int {
        self.logger.log("Processing");
        return self.value * 2;
    }
}

let logger = ImplC60456Ur();
let obj = C33762Ka(val: 50, logger: logger);
print(obj.process());
