// OOP: Builder Pattern
class BaseC33152BdBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> BaseC33152BdBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> BaseC33152BdBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> BaseC33152Bd {
        return BaseC33152Bd(name: self.name, age: self.age, email: self.email);
    }
}

class BaseC33152Bd {
    public let name: str
    public let age: int
    public let email: str
    
    public def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = BaseC33152BdBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
