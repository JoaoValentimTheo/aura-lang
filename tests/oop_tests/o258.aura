// OOP: Builder Pattern
class AbstractC25160YvBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> AbstractC25160YvBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> AbstractC25160YvBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> AbstractC25160Yv {
        return AbstractC25160Yv(name: self.name, age: self.age, email: self.email);
    }
}

class AbstractC25160Yv {
    public let name: str
    public let age: int
    public let email: str
    
    public def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = AbstractC25160YvBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
