// OOP: Builder Pattern
class BaseC65191FuBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> BaseC65191FuBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> BaseC65191FuBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> BaseC65191Fu {
        return BaseC65191Fu(name: self.name, age: self.age, email: self.email);
    }
}

class BaseC65191Fu {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = BaseC65191FuBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
