// OOP: Builder Pattern
class BaseC40729HsBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> BaseC40729HsBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> BaseC40729HsBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> BaseC40729Hs {
        return BaseC40729Hs(name: self.name, age: self.age, email: self.email);
    }
}

class BaseC40729Hs {
    public let name: str
    public let age: int
    public let email: str
    
    public def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = BaseC40729HsBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
