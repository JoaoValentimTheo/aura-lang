// OOP: Builder Pattern
class RealC11598TlBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> RealC11598TlBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> RealC11598TlBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> RealC11598Tl {
        return RealC11598Tl(name: self.name, age: self.age, email: self.email);
    }
}

class RealC11598Tl {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = RealC11598TlBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
