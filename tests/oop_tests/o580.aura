// OOP: Builder Pattern
class C62592SsBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> C62592SsBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> C62592SsBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> C62592Ss {
        return C62592Ss(name: self.name, age: self.age, email: self.email);
    }
}

class C62592Ss {
    public let name: str
    public let age: int
    public let email: str
    
    public def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = C62592SsBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
