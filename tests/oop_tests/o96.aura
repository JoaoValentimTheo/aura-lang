// OOP: Delegation
class C37642Fc {
    public let data: int = 0
    public def process() -> int { return self.data * 2; }
}

class C93728Ip {
    private let delegate: C37642Fc
    
    public def new(t: C37642Fc) { self.delegate = t; }
    
    public def process() -> int {
        return self.delegate.process() + 10;
    }
}

let target_obj = C37642Fc();
target_obj.data = 25;
let delegator_obj = C93728Ip(t: target_obj);
print(delegator_obj.process());
