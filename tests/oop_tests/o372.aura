// OOP: Mixin-like Composition
class AbstractC32835Dv {
    public def log(msg: str) { print(msg); }
}

class RealC97882Xm {
    private let logger: AbstractC32835Dv
    public let value: int
    
    def new(val: int, logger: AbstractC32835Dv) {
        self.value = val;
        self.logger = logger;
    }
    
    public def process() -> int {
        self.logger.log("Processing");
        return self.value * 2;
    }
}

let logger = AbstractC32835Dv();
let obj = RealC97882Xm(val: 50, logger: logger);
print(obj.process());
