// OOP: Mixin-like Composition
class RealC63779Xf {
    public def log(msg: str) { print(msg); }
}

class ImplC85931Aq {
    private let logger: RealC63779Xf
    public let value: int
    
    public def new(val: int, logger: RealC63779Xf) {
        self.value = val;
        self.logger = logger;
    }
    
    public def process() -> int {
        self.logger.log("Processing");
        return self.value * 2;
    }
}

let logger = RealC63779Xf();
let obj = ImplC85931Aq(val: 50, logger: logger);
print(obj.process());
