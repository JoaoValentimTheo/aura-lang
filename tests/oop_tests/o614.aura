// OOP: Delegation
class AbstractC48097Mp {
    public let data: int = 0
    public def process() -> int { return self.data * 2; }
}

class C95821Wn {
    private let delegate: AbstractC48097Mp
    
    def new(t: AbstractC48097Mp) { self.delegate = t; }
    
    public def process() -> int {
        return self.delegate.process() + 10;
    }
}

let target_obj = AbstractC48097Mp();
target_obj.data = 25;
let delegator_obj = C95821Wn(t: target_obj);
print(delegator_obj.process());
