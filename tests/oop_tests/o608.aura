// OOP: Builder Pattern
class BaseC66413AtBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> BaseC66413AtBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> BaseC66413AtBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> BaseC66413At {
        return BaseC66413At(name: self.name, age: self.age, email: self.email);
    }
}

class BaseC66413At {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = BaseC66413AtBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
