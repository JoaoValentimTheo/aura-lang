// OOP: Builder Pattern
class BaseC33869EgBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> BaseC33869EgBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> BaseC33869EgBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> BaseC33869Eg {
        return BaseC33869Eg(name: self.name, age: self.age, email: self.email);
    }
}

class BaseC33869Eg {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = BaseC33869EgBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
