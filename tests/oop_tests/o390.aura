// OOP: Delegation
class AbstractC87637Ed {
    public let data: int = 0
    public def process() -> int { return self.data * 2; }
}

class C28424My {
    private let delegate: AbstractC87637Ed
    
    def new(t: AbstractC87637Ed) { self.delegate = t; }
    
    public def process() -> int {
        return self.delegate.process() + 10;
    }
}

let target_obj = AbstractC87637Ed();
target_obj.data = 25;
let delegator_obj = C28424My(t: target_obj);
print(delegator_obj.process());
