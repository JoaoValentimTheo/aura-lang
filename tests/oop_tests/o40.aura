// OOP: Delegation
class MockC87503Xh {
    public let data: int = 0
    public def process() -> int { return self.data * 2; }
}

class AbstractC39602Wi {
    private let delegate: MockC87503Xh
    
    public def new(t: MockC87503Xh) { self.delegate = t; }
    
    public def process() -> int {
        return self.delegate.process() + 10;
    }
}

let target_obj = MockC87503Xh();
target_obj.data = 25;
let delegator_obj = AbstractC39602Wi(t: target_obj);
print(delegator_obj.process());
