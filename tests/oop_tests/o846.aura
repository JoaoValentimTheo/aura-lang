// OOP: Builder Pattern
class ConcreteC40842EmBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> ConcreteC40842EmBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> ConcreteC40842EmBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> ConcreteC40842Em {
        return ConcreteC40842Em(name: self.name, age: self.age, email: self.email);
    }
}

class ConcreteC40842Em {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = ConcreteC40842EmBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
