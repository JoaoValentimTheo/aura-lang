// OOP: Builder Pattern
class RealC21916TyBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> RealC21916TyBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> RealC21916TyBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> RealC21916Ty {
        return RealC21916Ty(name: self.name, age: self.age, email: self.email);
    }
}

class RealC21916Ty {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = RealC21916TyBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
