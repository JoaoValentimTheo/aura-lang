// OOP: Abstract Patterns
class BaseC53373Uc {
    public def process(x: int) -> int { return x; }
}

class ImplC56547Yu extends BaseC53373Uc {
    public def process(x: int) -> int { return x * 2; }
}

class ImplC83070Nc extends BaseC53373Uc {
    public def process(x: int) -> int { return x + 10; }
}

let obj1 = ImplC56547Yu();
let obj2 = ImplC83070Nc();

print(obj1.process(5));
print(obj2.process(5));
