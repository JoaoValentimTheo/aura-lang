// OOP: Builder Pattern
class C33689TsBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> C33689TsBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> C33689TsBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> C33689Ts {
        return C33689Ts(name: self.name, age: self.age, email: self.email);
    }
}

class C33689Ts {
    public let name: str
    public let age: int
    public let email: str
    
    public def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = C33689TsBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
