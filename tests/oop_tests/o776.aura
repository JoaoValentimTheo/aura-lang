// OOP: Builder Pattern
class AbstractC45334QiBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> AbstractC45334QiBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> AbstractC45334QiBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> AbstractC45334Qi {
        return AbstractC45334Qi(name: self.name, age: self.age, email: self.email);
    }
}

class AbstractC45334Qi {
    public let name: str
    public let age: int
    public let email: str
    
    public def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = AbstractC45334QiBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
