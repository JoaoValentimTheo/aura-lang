// OOP: Builder Pattern
class ImplC33320XaBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> ImplC33320XaBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> ImplC33320XaBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> ImplC33320Xa {
        return ImplC33320Xa(name: self.name, age: self.age, email: self.email);
    }
}

class ImplC33320Xa {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = ImplC33320XaBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
