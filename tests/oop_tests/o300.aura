// OOP: Builder Pattern
class MockC96686OzBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> MockC96686OzBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> MockC96686OzBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> MockC96686Oz {
        return MockC96686Oz(name: self.name, age: self.age, email: self.email);
    }
}

class MockC96686Oz {
    public let name: str
    public let age: int
    public let email: str
    
    public def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = MockC96686OzBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
