// OOP: Delegation
class AbstractC60889Aj {
    public let data: int = 0
    public def process() -> int { return self.data * 2; }
}

class ImplC94752Ig {
    private let delegate: AbstractC60889Aj
    
    def new(t: AbstractC60889Aj) { self.delegate = t; }
    
    public def process() -> int {
        return self.delegate.process() + 10;
    }
}

let target_obj = AbstractC60889Aj();
target_obj.data = 25;
let delegator_obj = ImplC94752Ig(t: target_obj);
print(delegator_obj.process());
