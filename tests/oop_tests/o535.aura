// OOP: Method Variants
class BaseC85327Lp {
    public let x: int
    public let y: int
    
    def new(x: int, y: int) {
        self.x = x;
        self.y = y;
    }
    
    // Expression method
    public def add() -> int = self.x + self.y
    
    // Block method
    public def multiply() -> int {
        return self.x * self.y;
    }
    
    // Computed method
    public def sum_squares() -> int {
        let sum = self.x * self.x + self.y * self.y;
        return sum;
    }
}

let obj = BaseC85327Lp(x: 38, y: 25);
print(obj.add());
print(obj.multiply());
print(obj.sum_squares());
