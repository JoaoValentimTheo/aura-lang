// OOP: Delegation
class ImplC82702Sw {
    public let data: int = 0
    public def process() -> int { return self.data * 2; }
}

class MockC75730Yx {
    private let delegate: ImplC82702Sw
    
    def new(t: ImplC82702Sw) { self.delegate = t; }
    
    public def process() -> int {
        return self.delegate.process() + 10;
    }
}

let target_obj = ImplC82702Sw();
target_obj.data = 25;
let delegator_obj = MockC75730Yx(t: target_obj);
print(delegator_obj.process());
