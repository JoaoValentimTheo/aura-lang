// OOP: Builder Pattern
class ImplC30355GbBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> ImplC30355GbBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> ImplC30355GbBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> ImplC30355Gb {
        return ImplC30355Gb(name: self.name, age: self.age, email: self.email);
    }
}

class ImplC30355Gb {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = ImplC30355GbBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
