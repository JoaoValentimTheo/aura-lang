// OOP: Mixin-like Composition
class AbstractC71537Si {
    public def log(msg: str) { print(msg); }
}

class AbstractC64170Jt {
    private let logger: AbstractC71537Si
    public let value: int
    
    def new(val: int, logger: AbstractC71537Si) {
        self.value = val;
        self.logger = logger;
    }
    
    public def process() -> int {
        self.logger.log("Processing");
        return self.value * 2;
    }
}

let logger = AbstractC71537Si();
let obj = AbstractC64170Jt(val: 50, logger: logger);
print(obj.process());
