// OOP: Mixin-like Composition
class AbstractC90920Na {
    public def log(msg: str) { print(msg); }
}

class MockC95683Hj {
    private let logger: AbstractC90920Na
    public let value: int
    
    public def new(val: int, logger: AbstractC90920Na) {
        self.value = val;
        self.logger = logger;
    }
    
    public def process() -> int {
        self.logger.log("Processing");
        return self.value * 2;
    }
}

let logger = AbstractC90920Na();
let obj = MockC95683Hj(val: 50, logger: logger);
print(obj.process());
