// OOP: Builder Pattern
class MockC68060OpBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> MockC68060OpBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> MockC68060OpBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> MockC68060Op {
        return MockC68060Op(name: self.name, age: self.age, email: self.email);
    }
}

class MockC68060Op {
    public let name: str
    public let age: int
    public let email: str
    
    public def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = MockC68060OpBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
