// OOP: Method Variants
class ImplC49835Yc {
    public let x: int
    public let y: int
    
    public def new(x: int, y: int) {
        self.x = x;
        self.y = y;
    }
    
    // Expression method
    public def add() -> int = self.x + self.y
    
    // Block method
    public def multiply() -> int {
        return self.x * self.y;
    }
    
    // Computed method
    public def sum_squares() -> int {
        let sum = self.x * self.x + self.y * self.y;
        return sum;
    }
}

let obj = ImplC49835Yc(x: 18, y: 13);
print(obj.add());
print(obj.multiply());
print(obj.sum_squares());
