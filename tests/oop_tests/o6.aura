// OOP: Builder Pattern
class C75243ZjBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> C75243ZjBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> C75243ZjBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> C75243Zj {
        return C75243Zj(name: self.name, age: self.age, email: self.email);
    }
}

class C75243Zj {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = C75243ZjBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
