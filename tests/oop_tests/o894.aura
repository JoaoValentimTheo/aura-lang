// OOP: Delegation
class AbstractC52788Em {
    public let data: int = 0
    public def process() -> int { return self.data * 2; }
}

class RealC53788Cv {
    private let delegate: AbstractC52788Em
    
    public def new(t: AbstractC52788Em) { self.delegate = t; }
    
    public def process() -> int {
        return self.delegate.process() + 10;
    }
}

let target_obj = AbstractC52788Em();
target_obj.data = 25;
let delegator_obj = RealC53788Cv(t: target_obj);
print(delegator_obj.process());
