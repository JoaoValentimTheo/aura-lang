// OOP: Builder Pattern
class C16519XwBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> C16519XwBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> C16519XwBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> C16519Xw {
        return C16519Xw(name: self.name, age: self.age, email: self.email);
    }
}

class C16519Xw {
    public let name: str
    public let age: int
    public let email: str
    
    public def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = C16519XwBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
