// OOP: Builder Pattern
class BaseC88139EnBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> BaseC88139EnBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> BaseC88139EnBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> BaseC88139En {
        return BaseC88139En(name: self.name, age: self.age, email: self.email);
    }
}

class BaseC88139En {
    public let name: str
    public let age: int
    public let email: str
    
    public def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = BaseC88139EnBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
