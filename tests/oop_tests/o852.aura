// OOP: Delegation
class RealC52461Nz {
    public let data: int = 0
    public def process() -> int { return self.data * 2; }
}

class ImplC73073Fr {
    private let delegate: RealC52461Nz
    
    def new(t: RealC52461Nz) { self.delegate = t; }
    
    public def process() -> int {
        return self.delegate.process() + 10;
    }
}

let target_obj = RealC52461Nz();
target_obj.data = 25;
let delegator_obj = ImplC73073Fr(t: target_obj);
print(delegator_obj.process());
