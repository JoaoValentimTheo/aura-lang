// OOP: Builder Pattern
class BaseC10445OxBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> BaseC10445OxBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> BaseC10445OxBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> BaseC10445Ox {
        return BaseC10445Ox(name: self.name, age: self.age, email: self.email);
    }
}

class BaseC10445Ox {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = BaseC10445OxBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
