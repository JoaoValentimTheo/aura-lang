// OOP: Delegation
class AbstractC53239Fc {
    public let data: int = 0
    public def process() -> int { return self.data * 2; }
}

class RealC24732Zz {
    private let delegate: AbstractC53239Fc
    
    def new(t: AbstractC53239Fc) { self.delegate = t; }
    
    public def process() -> int {
        return self.delegate.process() + 10;
    }
}

let target_obj = AbstractC53239Fc();
target_obj.data = 25;
let delegator_obj = RealC24732Zz(t: target_obj);
print(delegator_obj.process());
