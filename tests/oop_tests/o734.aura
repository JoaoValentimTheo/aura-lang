// OOP: Builder Pattern
class RealC88895UwBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> RealC88895UwBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> RealC88895UwBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> RealC88895Uw {
        return RealC88895Uw(name: self.name, age: self.age, email: self.email);
    }
}

class RealC88895Uw {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = RealC88895UwBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
