// OOP: Builder Pattern
class ConcreteC29645GmBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> ConcreteC29645GmBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> ConcreteC29645GmBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> ConcreteC29645Gm {
        return ConcreteC29645Gm(name: self.name, age: self.age, email: self.email);
    }
}

class ConcreteC29645Gm {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = ConcreteC29645GmBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
