// OOP: Builder Pattern
class ConcreteC73880WxBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> ConcreteC73880WxBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> ConcreteC73880WxBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> ConcreteC73880Wx {
        return ConcreteC73880Wx(name: self.name, age: self.age, email: self.email);
    }
}

class ConcreteC73880Wx {
    public let name: str
    public let age: int
    public let email: str
    
    def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = ConcreteC73880WxBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
