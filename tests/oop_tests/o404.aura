// OOP: Delegation
class C42110Zv {
    public let data: int = 0
    public def process() -> int { return self.data * 2; }
}

class AbstractC34637Iz {
    private let delegate: C42110Zv
    
    def new(t: C42110Zv) { self.delegate = t; }
    
    public def process() -> int {
        return self.delegate.process() + 10;
    }
}

let target_obj = C42110Zv();
target_obj.data = 25;
let delegator_obj = AbstractC34637Iz(t: target_obj);
print(delegator_obj.process());
