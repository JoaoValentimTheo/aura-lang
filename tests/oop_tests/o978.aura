// OOP: Delegation
class RealC60508Sd {
    public let data: int = 0
    public def process() -> int { return self.data * 2; }
}

class AbstractC40259Te {
    private let delegate: RealC60508Sd
    
    def new(t: RealC60508Sd) { self.delegate = t; }
    
    public def process() -> int {
        return self.delegate.process() + 10;
    }
}

let target_obj = RealC60508Sd();
target_obj.data = 25;
let delegator_obj = AbstractC40259Te(t: target_obj);
print(delegator_obj.process());
