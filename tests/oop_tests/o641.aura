// OOP: Composition
class MockC53427Xi {
    public let data: int = 57
    public def process() -> int { return self.data * 2; }
}

class AbstractC69676Jl {
    private let component: MockC53427Xi
    
    def new(comp: MockC53427Xi) {
        self.component = comp;
    }
    
    public def transform() -> int {
        return self.component.process() + 10;
    }
}

let inner_obj = MockC53427Xi(data: 57);
let outer_obj = AbstractC69676Jl(comp: inner_obj);
print(outer_obj.transform());
