// OOP: Builder Pattern
class MockC29298CgBuilder {
    private let name: str = ""
    private let age: int = 0
    private let email: str = ""
    
    public def with_name(n: str) -> MockC29298CgBuilder {
        self.name = n;
        return self;
    }
    
    public def with_age(a: int) -> MockC29298CgBuilder {
        self.age = a;
        return self;
    }
    
    public def build() -> MockC29298Cg {
        return MockC29298Cg(name: self.name, age: self.age, email: self.email);
    }
}

class MockC29298Cg {
    public let name: str
    public let age: int
    public let email: str
    
    public def new(name: str, age: int, email: str = "") {
        self.name = name;
        self.age = age;
        self.email = email;
    }
}

let obj = MockC29298CgBuilder().with_name("Aura").with_age(30).build();
print(obj.name);
print(obj.age);
