// Collections: Deduplication
let items = [1, 2, 2, 3, 3, 3, 4, 4, 4, 4];
let mut seen = [];
let mut unique = [];

for item in items {
    unless seen |> contains(item) {
        seen = [*seen, item];
        unique = [*unique, item];
    }
}

print(unique);
