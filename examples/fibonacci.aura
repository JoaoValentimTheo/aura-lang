def fibonacci(n) -> int {
  let mut a = 0
  let mut b = 1
  for i in range(n) {
    let next = a + b
    a = b
    b = next
  }
  return a
}

def main() {
  for i in range(10) {
    print(f"fib({i}) = {fibonacci(i)}")
  }
}